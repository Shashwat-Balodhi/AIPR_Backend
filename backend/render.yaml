services:
  - type: web
    name: aipr-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: CHUNKR_API_KEY
        sync: false
      - key: OPENROUTER_API_KEY
        sync: false
